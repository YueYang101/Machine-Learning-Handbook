# 本仓库的编写规则

**动手改任何东西之前，先完整读一遍 [`STYLE.md`](STYLE.md)。** 那是本仓库唯一的规则来源，本文件只是指针。

`STYLE.md` 覆盖：页面分类规则（理论 / 实验、以及新页归到哪一类的判据）、跳转与导航契约、
页面结构模板与共享 CSS 变量、写作规则、图表规则、外部导入页面的处理、发布前检查清单、Git 约定。

最容易被违反、后果最大的五条（细节仍以 `STYLE.md` 为准）：

1. **新页面必须在同一次改动里登记进 `index.html`**，并归到正确的分类下，否则等于没发布。
2. **每页顶部和 footer 都要有回 `index.html` 的链接**；有上级页时先上级、再主页。
3. **相关研究页与它所属的方法页必须双向链接**，并用一句话说清两者的区别。
4. **连续两个 `$$` 公式之间必须有解释段**（`.why`），说明「为什么必须走这一步」——只罗列变形不合格。
5. **所有颜色走 CSS 变量**，深浅色模式都要能读；动画按解析解实时模拟，不用示意贴图。

改完后按 `STYLE.md` 第 8 节的清单自检，包括跑一遍链接/锚点校验脚本。
不要主动 `git push`，除非用户明确要求。
